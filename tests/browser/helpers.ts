// Let Vite resolve dependencies exactly as it does for the application, including cache keys.
export { PDFDocument } from 'pdf-lib'
export { openPdfForEditing, validatePdfOutput } from '../../src/lib/fileCompatibility/pdf'
